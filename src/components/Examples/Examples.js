import React from 'react';

const Examples = ({handleChange}) => {
    console.log(handleChange);
    return (
        <div>
            <h2>Examples</h2>
        </div>
    );
};

export default Examples;